# Worklog: all unblocked release + PR-follow-up work

Started: 2026-06-09T22:46-07:00
Mode: overnight
Plan: plans/overnight-2026-06-09.md (tonight), plans/app-store-release-design.md (release, living)
Branch: rae/doctor-app
Constraints: no push, no PRs, no destructive ops.

## Goal (from Rae, via /overnight prompt)

"All unblocked work": every remaining app-store-release item not blocked
on accounts or devices, plus the six pr-notes follow-ups. Toolchain
decision: install dx + Android SDK pieces on this machine (user-local);
iOS native builds stay blocked here (no Xcode, App Store login needed).

## Environment finding (22:46)

This Mac mini (M1, macOS 14.7) is NOT the machine the 2026-06-01 mobile
session ran on: no dx, no Xcode (CommandLineTools only, so no actool and
no iOS SDK), no Android Studio JBR, SDK platforms <= 32, NDK <= 25.2.
Approved remedy: cargo install dioxus-cli@0.7.9 + sdkmanager pulls of
platform 35, build-tools 35.0.0, NDK 28.2, platform-tools. Homebrew
OpenJDK 19 stands in for the missing Android Studio JBR.

## Progress

### 22:46 - Session start
Read both prior worklogs (goals met), the release plan, pr-notes,
workflow/writing/rust/iroh skills. Confirmed goal and toolchain installs
with Rae before she left. Kicked off in background: dioxus-cli install,
SDK component downloads, baseline `cargo test --workspace` on this
machine. Writing tonight's plan next.

### 22:58 - Toolchain + plan + adversarial plan review
SDK installs finished (platform-35, build-tools 35.0.0, NDK r28c,
platform-tools 37). dx still compiling; baseline tests still running.
Plan written (plans/overnight-2026-06-09.md). Adversarial findings:

- A4 may be a no-op by design: the release plan's own finding 2 says
  presets::N0 uses no mDNS, so CHANGE_WIFI_MULTICAST_STATE and a
  MulticastLock would be dead permissions Play reviewers flag. The
  right outcome may be "verify no mDNS on Android, document why the
  permission is omitted, confirm INTERNET is present." Research first,
  do not cargo-cult the checklist.
- B3 scope check: if port-variation collection turns out to need
  upstream iroh net-report changes, that is a real external blocker;
  anything implementable inside this workspace is in scope tonight.
- B1 CI must stay cheap: a compile check of the app crate, not a full
  dx mobile bundle on a paid macOS runner.
- B2 keeps QUIC path RTT for the per-path table; only the headline
  latency series moves to the probe ping loop.
- A8 splash: dx 0.7.9 may not expose a launch-screen hook; wrapper
  injection is the fallback, same pattern as icons.

Resolutions adopted into the plan. Dispatching three parallel
subagents (B3 research, B1 CI, A10+A11 docs) while I take B5 + B4.

### 23:25 - B5, B4, B1, A10+A11, A12 closed (5 commits)

- B5 (6856505): cli/log.txt untracked + ignored.
- B4 verified e2e on the real network: cli accept + cli connect on this
  machine (distinct keys; same-key dialing is refused as self-dial,
  noted). Hole punch reached `direct`, path table showed LAN-direct
  0.5 ms selected vs relay 57.5 ms, ttfdb 154 ms, live latency series
  n=31 (min 1.3 / avg 7.3 / max 148.4 ms), throughput 120.2 Mbps.
  Caveat: the dashboard hides itself on non-TTY stderr (indicatif), so
  evidence was captured under a PTY (script + SIGALRM).
- B1 (ca1b6a1): app CI job on macos-latest (clippy + test, default
  desktop feature); subagent-built, reviewed against the existing
  workflow, stale exclusion comments updated.
- A12 (acfbf14, fix!): found via the compliance-docs subagent and
  verified in code: fresh installs were registering with iroh-services
  and pushing metrics every 60 s through the bundled key, contradicting
  every privacy doc. App-side empty override now resolves to off; cli
  keeps the bundled default. Tests/fmt/clippy green.
- A10+A11 (fe3c77b): nutrition-label + Data Safety answers and a terms
  draft, updated to the post-fix premise.

Toolchain: dx 0.7.9 + SDK 35 + NDK r28c installed. dx-based Android
build (T0/A7) deliberately deferred behind the remaining interactive
cargo work so the cross-compile does not hold the target-dir lock;
runs next alongside docs/assets work.

### 23:38 - A12 follow-through, B2, A1-A11 subagent integration, B3 landed

- A12 (acfbf14 fix!, fe3c77b docs): the compliance-docs subagent
  found, and I verified in code, that fresh installs registered with
  iroh-services and pushed metrics every 60 s through the bundled key.
  App-side empty override now resolves to off; cli keeps its default.
  All store docs reconciled to the (now true) opt-in premise.
- Logging polish (616842d): fresh cli installs printed "Error reading
  the log directory/files" because tracing-appender prunes a directory
  that does not exist yet; created up front, verified with a fresh
  IROH_DATA_DIR.
- B2 (8c6be8b): node owns the latency series now; dials plot probe
  ping round-trips (identical to cli connect), incoming probes keep
  path RTT (identical to cli accept). 53->55 app tests green.
- Three parallel subagents integrated and committed after review:
  a90eb11 (A1+A2 first-launch UX: trust note, services probes skipped
  without a key, export errors surfaced, invalid-id hint, empty-state
  guidance), dc89c2f (A3-A6: iOS floor 13.0, no cargo-culted Android
  permissions with evidence from presets::N0 and netwatch, version
  strategy + foreground-only decisions documented), a1cf12e (A8+A9:
  launch storyboard fixing dx's blank-launch-screen hole, Android 12
  splash theme, Play feature graphic, real favicon).
- B3 (4950c36): the night's headline. Research subagent's multi-relay
  design was REJECTED on opposing-stance review (different relays test
  dest-address variation, which the base report already collects; RFC
  4787 port variation needs one host on two ports, which no public
  iroh infra offers: relays serve QAD only on 7842). Shipped both
  halves instead, matching the doctor's two-party model: a QadHelper
  server (noq + self-signed cert, certificate deliberately unverified,
  STUN-grade trust documented) and a same-socket multi-target probe in
  core; cli gains `nat-helper` and `diagnostics --nat-probe`. Verified
  end to end against a local helper pair: `NAT | Easy` printed for the
  first time, helpers observed the identical mapping. Workspace suite
  100 tests green (12 cli + 55 app + 33 core), clippy 0, fmt clean.
- App-side --nat-probe UX is NOT in tonight's scope on purpose: it
  needs a product decision on where helper addresses live in the UI;
  pr-notes item 5 as written asks for collection + classification,
  which the cli now does. Recorded here, not silently dropped.
- A7 in progress: wrapper gained BUILD_NUMBER stamping (CFBundleVersion
  + versionCode) and a bundleRelease step per the A6 strategy; branded
  Android release build + AAB running in background on this machine
  (first dx run here: NDK r28c, homebrew JDK, SDK 35).

## Staff reviews

(none yet)

## Blockers

- iOS native build verification (dx build --ios, actool, sim/device
  screenshots): no Xcode on this machine; needs App Store login. All
  iOS work tonight is config/code/docs, with verify steps documented
  for the build machine.
- Store screenshots: need a device or simulator/emulator; tracked as a
  stretch goal via Android emulator only if everything else closes.

## Decisions made

- Install user-local toolchain (dx, SDK 35, NDK 28) per Rae's explicit
  choice tonight.

## Summary

(in progress)
